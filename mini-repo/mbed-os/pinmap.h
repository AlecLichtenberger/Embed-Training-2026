#pragma once
#include "PinNames.h"
// Real mbed header exposes pin_function()/pinmap_pinout() for low-level HAL
// pin muxing. Nothing at this level of the training exercise needs actual
// pin muxing, so this is intentionally empty beyond pulling in PinNames.h.
