#pragma once
// Real mbed header defines target-specific ticker bit-widths/frequencies.
// Nothing in this training slice reads those constants directly, so this
// is intentionally empty. If a compile error says otherwise, add the
// specific macro it's asking for here.
